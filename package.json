{
  "name": "ocean",
  "private": true,
  "scripts": {
    "package": "npx vsce package",
    "publish": "npx vsce publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@vscode/vsce": "^2.31.1"
  }
}
